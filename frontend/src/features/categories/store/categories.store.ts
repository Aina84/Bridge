// src/features/categories/store/categories.store.ts
// Categories are managed inside tickets.store.ts to keep shared state.
// This file is intentionally minimal — import from tickets.store if needed.
export {};
